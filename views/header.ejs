<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Header</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
  <% if(messages && messages.error && messages.error.length != 0) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative text-center mb-3" role="alert">
     <strong> <%=messages.error %> </strong>
   </div>
   <% } %>
     <% if(messages && messages.success && messages.success.length != 0) { %>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative text-center mb-3" role="alert">
     <strong> <%=messages.success %> </strong>
   </div>
   <% } %>
    <div class="text-4xl bold block">
        Online Voting Platform<a
          class="mx-20 text-xl rounded bg-green-700 font-medium p-2 outline-none text-white"
          href="/Signout"
          >SignOut</a
        >
      </div>
      <div class="my-10 text-3xl text-black blcok">Welcome <%= User %>...</div>
</body>
</html>