<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Declare Election</title>
    <meta name="csrf-token" content="<%= csrfToken %>" />
</head>
<body>
    <div class="grid justify-center mt-5">
        <%-include ("header")%>
        <div class="text-2xl font-medium">Title:<%=electionList.Title%></div>
        <form action="/url/<%=ElectionId%>" method="post" class="mt-10">
            <input type="hidden" name="_csrf" value="<%= csrfToken%>" />
            <span class="text-2xl font-medium block">Enter Url:</span>
            <input type="text" name="Url" placeholder="Enter Url(e.x. live/voting)" class="mt-2 rounded border border-black outline-none p-3 w-2/4">
            <input
            type="submit"
            value="Add"
            class="my-4 text-xl rounded bg-green-700 font-medium p-1 outline-none text-white w-20 h-12 cursor-pointer block"
          />
        </form>
    </div>
</body>
</html>